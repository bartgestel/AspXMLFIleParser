@using Microsoft.Extensions.Logging
@{
    ViewData["Title"] = "XML";
}


<h1>XML</h1>
<div class="mb-3 mt-3">
    <form method="post" enctype="multipart/form-data" asp-controller="Home" asp-action="Xml">
        <div>
            <label class="form-label">Upload XML File</label>
            <input type="file" name="xmlFile" class="form-control"/>
            <input type="submit" value="Upload" class="btn btn-primary mt-3" />
        </div>
    </form>
</div>

@if (Model != null)
{
    <h1>@Model.xmlns</h1>
    <div class="container ">
        <div class="row">
            <div class="col">
                <ul class="list-group ">
                    @foreach (var dataCollectionPlan in Model.DataCollectionPlan)
                    {
                        <li class="list-group-item" id="@dataCollectionPlan.Id" onclick="PickEvent('@dataCollectionPlan.Id')">
                            <h3>@dataCollectionPlan.Name</h3>
                            <p>@dataCollectionPlan.Id</p>
                            <p>@dataCollectionPlan.EventRequest.EventId</p>
                        </li>
                    }
                </ul>
            </div>
            <div class="col">
                <ul class="list-group" id="parametersList">

                </ul>
            </div>
        </div>
    </div>

    <script>
        function PickEvent(eventId) {
            @foreach (var dataCollectionPlan in Model.DataCollectionPlan)
            {

            }
        }
    </script>
}
